# rbxm-api
HTTP service that turns instance trees into real .rbxm files (and back) using rbx-dom, plus a Luau RBXM ModuleScript that talks to it. Lets normal (non-plugin) server scripts do what SerializationService can't.
